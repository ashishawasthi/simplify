# GitGuardian secret scanning. The one ignored file holds the Firebase web
# app's config, whose apiKey is a public identifier Google designs to be
# shipped in web pages (access is controlled by the Firestore and Storage
# rules and by the Cloud Functions' own checks, not by keeping it secret).
version: 2
secret:
  ignored-paths:
    - public/coach/js/firebase-config.js
