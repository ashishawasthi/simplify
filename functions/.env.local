# Read ONLY by the Functions emulator (never deployed: firebase.json ignores *.local).
# Deterministic fake answers instead of Gemini Flash and Omni (functions/ai.js),
# so the coach app can be tried end to end offline and for free. Remove this
# line to call the real models from the emulator (it will cost money).
SIMPLIFY_AI_FAKE=1
